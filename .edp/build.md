# Build

**Goal:** Build a session continuity tool for AI-native development workflows that helps developers pick up where they left off across Claude sessions without re-explaining context. The tool should capture, store, and restore session state so each new Claude session starts with full context of what was happening, what decisions were made, and what's next.

---

## Implementation checklist

### Milestone 1 — Foundation
- [x] Scaffold Node.js project: `package.json`, `bin/sc`, `src/` directory
- [x] `src/utils.js` — shared helpers: git(), atomicWrite(), parseSessions(), serializeSessions(), trimToWords()
- [x] `src/init.js` — `sc init`: creates `.claude/`, session.md, wires @import into CLAUDE.md, registers Stop hook in settings.json, checks for claude CLI
- [x] `src/snapshot.js` — `sc snapshot`: collects git state, attempts `claude -p` narrative (15s timeout), prepends new entry, trims to 3 sessions, atomic write
- [x] `templates/narrative-prompt.md` — structured prompt template for claude -p
- [x] `bin/sc` — CLI entry point with help/version flags
- [x] `src/status.js` — `sc status`: pretty-prints session.md
- [x] `src/clear.js` — `sc clear`: resets with confirmation
- [x] `src/rotate.js` — `sc rotate`: manual snapshot trigger

### Milestone 2 — Validate end-to-end
- [ ] Run `sc init` in a real project, end a session, verify session.md populates
- [ ] Verify new session opens with context from CLAUDE.md @import
- [ ] Test `sc status`, `sc clear`, `sc rotate`

### Milestone 3 — Polish & ship
- [ ] Write README.md
- [ ] Test on 3 real projects
- [ ] Publish to npm as `session-continuity`

---

## AI coding prompts

### For the next milestone (M2 validation)
```
Test the session-continuity tool at /Users/geli/Desktop/session-continuity.

1. Run: cd <any git project> && node /Users/geli/Desktop/session-continuity/bin/sc init
2. Verify: .claude/session.md created, CLAUDE.md has @import, .claude/settings.json has Stop hook
3. Run: node /Users/geli/Desktop/session-continuity/bin/sc snapshot
4. Verify: .claude/session.md has a session entry with git state + timestamp
5. Run: node /Users/geli/Desktop/session-continuity/bin/sc status
6. Fix any issues found, report what worked and what didn't.
```

### For README
```
Write a README.md for session-continuity (the sc CLI tool) at /Users/geli/Desktop/session-continuity.
Cover: what it does (1 paragraph), install (npm install -g session-continuity), quickstart (sc init, how the hook works, sc status), how it works (diagram or prose), troubleshooting (claude CLI not found, hook not firing), .gitignore recommendation.
Keep it under 150 lines. No emojis.
```

---

## Implementation notes
- `snapshot.js` writes to stderr (not stdout) so hook output doesn't interfere with Claude's response pipeline
- Atomic write (tmp → rename) protects against partial writes on crash
- `claude -p` call uses `spawnSync` with 15s timeout; on any failure (exit code, error, empty output) falls back to objective-only with a `[unavailable]` note
- Sessions parsed by splitting on `^---$` lines; each serialized entry is prefixed with `---\n`
- `sc init` uses upsert logic — safe to run multiple times in the same project

## Files changed
- `package.json` — project manifest, bin alias, ESM type
- `bin/sc` — CLI entry point
- `src/utils.js` — shared constants and helpers
- `src/init.js` — sc init command
- `src/snapshot.js` — sc snapshot command (hook target)
- `src/status.js` — sc status command
- `src/clear.js` — sc clear command
- `src/rotate.js` — sc rotate command
- `templates/narrative-prompt.md` — claude -p prompt template

_Generated by edp_build — 2026-05-25T01:58:34.922Z_
