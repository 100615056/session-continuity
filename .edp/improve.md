# Improve

**Goal:** Build a session continuity tool for AI-native development workflows that helps developers pick up where they left off across Claude sessions without re-explaining context. The tool should capture, store, and restore session state so each new Claude session starts with full context of what was happening, what decisions were made, and what's next.

---

## Must-have improvements
<!-- Fixes that affect correctness, reliability, or core usability — do these now -->

- [ ] **Write README.md and publish to npm** — M3 is incomplete. Tool is not yet installable by others. Blocking the "fully met" verdict.
- [ ] **Surface `sc rotate` in `sc init` output** — crash safety relies on users knowing about `sc rotate` before force-quitting, but it's not mentioned after init. Add a one-liner to the init confirmation output.

## Should-have improvements
<!-- Meaningful quality improvements — do before next major release -->

- [x] **`sc decide "message"` command** — lets the user append a permanent decision mid-session that persists across the rolling window. Directly addresses the "why-choices survive" partial criterion. Writes to a `## Pinned decisions` block in `session.md` that `sc snapshot` never trims. *(implemented below)*
- [ ] **`PostToolUse` incremental checkpoint** — write a lightweight git-state-only snapshot on every Nth tool call (e.g. N=10). Protects against crash losing the full session. Stop hook still writes the full narrative on normal exit.
- [ ] **Test for `claude -p` unavailable path** — currently a documented gap. Add a test that stubs out the `claude` binary (or runs in a PATH-controlled temp env) and asserts objective-only output.

## Nice-to-have enhancements
<!-- Optional upgrades — only if time and priority allow -->

- [ ] **Configurable rolling window** — `sc config --max-sessions N` writes to `.claude/sc-config.json`. Default stays 3.
- [ ] **`sc history`** — lists all past session headers (dates, branches) from `session.md` in a compact table.
- [ ] **`sc diff`** — shows git diff between current state and the branch/commit recorded in the last snapshot.
- [ ] **SIGTERM/SIGINT trap in hook** — register a signal handler in the Stop hook script so force-quit (Ctrl+C) also triggers a snapshot.

## Out of scope
- Multi-user sharing or cloud sync (per original goal definition)
- GUI or web dashboard
- Support for non-Claude AI tools (Cursor, Copilot, etc.)

---

## Improvement prompts for AI coding tools

### `sc decide` implementation prompt
```
Add a `sc decide "message"` command to the session-continuity tool at /Users/geli/Desktop/session-continuity.

Behavior:
- Appends the message as a bullet to a `## Pinned decisions` section in `.claude/session.md`
- Creates the section if it doesn't exist (insert it before the first `---` session block)
- `sc snapshot` must preserve this section and never trim it (it is outside the rolling window)
- Writes atomically
- Prints confirmation: `Decision pinned: "<message>"`

Files to modify: src/decide.js (new), src/utils.js (export PINNED_SECTION constant), bin/sc (register command)
Test: src/decide.test.js — test that pinned section is created, appended to, and survives multiple sc snapshot calls
```

### `PostToolUse` checkpoint prompt
```
Add incremental checkpoint support to session-continuity at /Users/geli/Desktop/session-continuity.

Add a new hook entry to `sc init` that registers a PostToolUse hook firing every 10 tool calls.
The hook runs `sc checkpoint` — a lightweight command that writes ONLY git state (no claude -p call) to `.claude/session-checkpoint.md`.
On normal exit, `sc snapshot` reads `.claude/session-checkpoint.md` and merges its file list into the full snapshot, then deletes it.
This ensures crash safety: even if Stop hook doesn't fire, the last checkpoint is available.
```

_Generated by edp_improve — 2026-05-25T02:36:03.922Z_
